'use strict';

describe('myApp.editor module', function() {

  beforeEach(module('myApp.editor'));

  describe('editor controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('EditorCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});